version: '3.9'
services:
  server:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - .:/app
    env_file:
      - .env
    command: uv run uvicorn server.app:app --host 0.0.0.0 --port 8000 --reload
